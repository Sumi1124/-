name: Build SourceDesk

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    name: Build unsigned .app + .dmg
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Pre-build diagnostics
        run: |
          echo "=== Xcode version ==="
          xcodebuild -version
          echo "=== SDK ==="
          xcrun --show-sdk-path --sdk macosx
          echo "=== Project targets ==="
          xcodebuild -project SourceDesk.xcodeproj -list
          echo "=== Scheme ==="
          xcodebuild -project SourceDesk.xcodeproj -scheme SourceDesk -showBuildSettings -configuration Release | grep -E '(PRODUCT_NAME|TARGET_NAME|CODE_SIGN|SWIFT_VERSION|MACOSX_DEPLOYMENT)' | head -20

      - name: Build (Release, universal, unsigned)
        run: |
          set -o pipefail
          mkdir -p build
          xcodebuild \
            -project SourceDesk.xcodeproj \
            -target SourceDesk \
            -configuration Release \
            -derivedDataPath build \
            -destination 'platform=macOS' \
            ARCHS="arm64 x86_64" \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            INFOPLIST_KEY_CFBundleIconName=AppIcon \
            INFOPLIST_KEY_LSMinimumSystemVersion=14.0 \
            INFOPLIST_KEY_NSPrincipalClass=NSApplication \
            -quiet \
            build 2>&1 | tee build/xcodebuild.log
          echo "=== BUILD EXIT CODE: \${PIPESTATUS[0]} ==="
          cat build/xcodebuild.log
          test -d "build/Build/Products/Release/SourceDesk.app"

      - name: Upload build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build/xcodebuild.log
          if-no-files-found: warn

      - name: Package DMG
        run: |
          set -euxo pipefail
          out="build/dmgroot"
          mkdir -p "$out"
          cp -R "build/Build/Products/Release/SourceDesk.app" "$out/"
          ln -s /Applications "$out/Applications"
          hdiutil create \
            -volname "SourceDesk" \
            -srcfolder "$out" \
            -ov \
            -format UDZO \
            "SourceDesk.dmg"

      - name: Verify DMG
        run: hdiutil verify SourceDesk.dmg

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: SourceDesk-dmg
          path: SourceDesk.dmg
          if-no-files-found: error

      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" SourceDesk.dmg \
            --title "SourceDesk $GITHUB_REF_NAME" \
            --notes "Universal macOS app (Apple Silicon + Intel). Unsigned — on first open, right-click the app and choose **Open**."
